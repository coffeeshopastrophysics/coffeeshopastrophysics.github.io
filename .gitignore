# Hugo build artifacts
/public/
/resources/_gen/
/.hugo_build.lock

# Node / Tailwind tooling
node_modules/
# (Site repo) ✅ Commit your package-lock.json for reproducible builds
# (Theme-only repo) You may choose to ignore lockfiles.

# If you ever output CSS via CLI into assets/static, ignore the generated files:
assets/css/*.generated.css
assets/css/tailwind.css
static/css/tailwind.css

# OS cruft
.DS_Store
Thumbs.db

# Editors / logs
.idea/
.vscode/
*.swp
*.swo
*.log

# Env files (if you ever add them)
.env
.env.*
